{
  "Python constructor with super": {
    "prefix": ["init"],
    "body": [
      "def __init__(self${1:, ${2:params}}):",
      "    super().__init__(${3})",
      "    ${0}"
    ],
    "description": "Python constructor that calls super().__init__ inside a class."
  },
  "Machine learning imports": {
    "prefix": ["mlimports"],
    "body": [
      "import torch",
      "import torch.nn as nn",
      "import torch.nn.functional as F",
      "import numpy as np",
      "import pandas as pd",
      "from tabulate import tabulate",
      ""
    ],
    "description": "Common ML/scientific Python import block."
  }
}
